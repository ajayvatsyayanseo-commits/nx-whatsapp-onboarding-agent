# Lightweight Extraction Prompt

Use only when deterministic parsing cannot identify simple fields from messy user text.

Return strict JSON with candidate values only. Do not validate, approve, or write data.
Final validation must remain in PHP validators.
